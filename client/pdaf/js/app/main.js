define(['jquery', 'd3', 'app/chart'],
    function($, d3, Chart) {
        console.log('chart?', Chart);
        $(function() {
            console.log('main loaded!', $('.pdaf-chart'));

            // var pdaf = pdaf || {};

            // pdaf.chart = chart();
            // pdaf.chart.initialize('pdaf-chart');
        });
    }
);